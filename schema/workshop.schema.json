{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "workshop.schema.json",
  "title": "Workshop edition",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "conference",
    "year",
    "topics"
  ],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 3,
      "maxLength": 200,
      "description": "Full official workshop name."
    },
    "acronym": {
      "type": "string",
      "maxLength": 40,
      "description": "Short name (e.g. 'MATH-AI'). Empty string if none."
    },
    "conference": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Conference id; must exist in data/conferences.yml."
    },
    "year": {
      "type": "integer",
      "minimum": 2014,
      "maximum": 2100
    },
    "website": {
      "type": "string",
      "pattern": "^https?://",
      "maxLength": 500,
      "description": "Official workshop website. Leave unset if unknown — the site shows a \"help us add it\" prompt."
    },
    "location": {
      "type": "string",
      "maxLength": 200,
      "description": "Where the workshop takes place, exactly as OpenReview publishes it. Stored raw — the values are free text and inconsistent (six spellings of Pittsburgh, one \"Sweeden\"), so spellings are folded together for display in lib/workshops.mjs rather than here. Keeping the source value means improving that later is a rebuild, not a data migration. Maintained by the weekly discovery sync; unset when OpenReview has none."
    },
    "topics": {
      "type": "array",
      "minItems": 1,
      "maxItems": 5,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "description": "Topic ids; each must exist in data/topics.yml."
    },
    "submission_deadline": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}( \\d{2}:\\d{2})?$",
      "description": "'YYYY-MM-DD' or 'YYYY-MM-DD HH:MM' (wall-clock time in `timezone`)."
    },
    "timezone": {
      "type": "string",
      "maxLength": 50,
      "description": "'AoE' (default), 'UTC', or an IANA name like 'America/Los_Angeles'."
    },
    "abstract_deadline": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}( \\d{2}:\\d{2})?$",
      "description": "Optional. Two-stage venues (~3% of OpenReview workshops) require a mandatory abstract registration BEFORE the paper deadline. Always UTC wall-clock, independent of `timezone`. `submission_deadline` stays the PAPER deadline (the headline and countdown), so the displayed date never rolls forward and mimic an extension; this is shown beside it and remains visible, marked closed, once passed. Auto-filled from the venue's OpenReview `Abstract Registration:` date."
    },
    "deadline_notes": {
      "type": "string",
      "maxLength": 300
    },
    "tracks": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "description": "Per-track submission deadlines for multi-track workshops (e.g. Full + Short). The headline deadline is derived as the soonest track still open; a track with no submission_deadline is shown as 'not yet announced'.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 80
          },
          "submission_deadline": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}( \\d{2}:\\d{2})?$"
          },
          "timezone": {
            "type": "string",
            "maxLength": 50
          }
        }
      }
    },
    "notification_date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "workshop_date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "openreview_venue_id": {
      "type": "string",
      "maxLength": 200,
      "description": "e.g. 'NeurIPS.cc/2024/Workshop/MATH-AI'. Enables the auto paper list."
    },
    "proceedings_url": {
      "type": "string",
      "pattern": "^https?://",
      "maxLength": 500
    },
    "submission_portal": {
      "type": "string",
      "enum": [
        "openreview",
        "cmt",
        "email",
        "other",
        "unknown"
      ]
    },
    "organizers": {
      "type": "array",
      "maxItems": 40,
      "items": {
        "type": "string",
        "maxLength": 120
      }
    },
    "previous_editions": {
      "type": "array",
      "maxItems": 30,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "year"
        ],
        "properties": {
          "year": {
            "type": "integer",
            "minimum": 2000,
            "maximum": 2100
          },
          "website": {
            "type": "string",
            "pattern": "^https?://"
          },
          "proceedings_url": {
            "type": "string",
            "pattern": "^https?://"
          }
        }
      }
    },
    "notes": {
      "type": "string",
      "maxLength": 1000
    },
    "added": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date this entry was added (used for the 'new workshops' RSS feed)."
    },
    "deadline_history": {
      "type": "array",
      "description": "Append-only log of observed submission-deadline values. Newest last. Written by the OpenReview sync; never hand-edited except to remove a false positive.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "value",
          "recorded"
        ],
        "properties": {
          "value": {
            "type": [
              "string",
              "null"
            ],
            "description": "Deadline as recorded (wall clock, same form as submission_deadline). null = announced with no date yet."
          },
          "recorded": {
            "type": "string",
            "format": "date",
            "description": "Date WE observed this value — not when organizers changed it."
          },
          "timezone": {
            "type": "string",
            "description": "Timezone the value was recorded in (e.g. AoE, UTC, or an IANA name). A wall-clock string only fixes an instant together with its zone, so each entry carries its own: re-parsing an old value under a zone the entry has since changed to shifts it by up to 12 hours. Omitted when value is null; when absent on older entries, readers fall back to the entry's current timezone."
          }
        }
      }
    },
    "review_ack": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        },
        "acronym": {
          "type": "string"
        },
        "website": {
          "type": "string"
        },
        "submission_deadline": {
          "type": "string",
          "description": "An OpenReview deadline value reviewed and deliberately not adopted (typically an earlier move you judged wrong or irrelevant). Compared as an instant, so formatting differences don't matter."
        }
      },
      "description": "Optional. Records an OpenReview value that was reviewed and deliberately NOT adopted, so the weekly review stops reporting it. Stores the rejected VALUE rather than a blanket mute: if OpenReview later changes to something different, it is reported again. Set by a human after a review decision."
    }
  }
}
