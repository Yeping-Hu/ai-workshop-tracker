node_modules/
site/node_modules/
site/dist/
.DS_Store
*.log

# Transient changelog the discovery workflow writes (never committed)
.deadline-changes.md

# Local Worker development: .dev.vars holds real secret values for
# `wrangler dev`, and .wrangler/ is its local state (including a local copy of
# the D1 database, which would contain subscriber addresses). Neither ever
# belongs in the repo.
.dev.vars
.wrangler/
alerts/worker/node_modules/

# Astro's per-machine update-check timestamp; the generated types beside it stay tracked
site/.astro/settings.json
site/.astro/preview.json
